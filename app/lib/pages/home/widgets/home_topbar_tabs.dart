import 'package:flutter/material.dart';

class HomeTopbarTabs extends StatelessWidget {
  const HomeTopbarTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
